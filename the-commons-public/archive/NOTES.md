# Archive notes

`README_v0_1_original.md` is the README that shipped with the original v0.1 starter package.

The public package intentionally excludes:

- local SQLite experiment databases;
- private persistent branch-session databases;
- virtual environments;
- API keys;
- private conversation logs.

The Markdown reports in `reports/` are the preserved run outputs used for the public summary.
