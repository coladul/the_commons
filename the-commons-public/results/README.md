# Machine-readable summaries

These CSV files are convenience summaries transcribed from the preserved Markdown reports.

The reports under `../reports/` are the source of record. If a CSV conflicts with a report, use the report.

- `version_summary.csv` — compact cross-version overview.
- `v0_5_aggregate.csv` — aggregate v0.5 condition results.
- `v0_5_paired_contrasts.csv` — paired v0.5 contrasts and bootstrap intervals.
